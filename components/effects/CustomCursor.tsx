/**
 * CustomCursor — disabled.
 *
 * The custom cursor has been removed in favour of the default browser cursor.
 * This file is kept as a null export so any dormant import doesn't break the
 * build. The component renders nothing and never sets cursor: none.
 */

export default function CustomCursor() {
  return null;
}
